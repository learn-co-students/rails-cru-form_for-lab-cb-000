<h1>Listing Genres</h1>

<% @genres.each do |genre| %>
  <div><td><%= link_to genre.name, genre_path(genre) %></div>
<% end %>

<br>

<%#= link_to "New artist", new_artist_path %>
<%= form_tag new_genre_path, method: :get do %>
  <div><%= submit_tag "New genre" %></div>
<% end %>
