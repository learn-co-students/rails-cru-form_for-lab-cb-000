<!DOCTYPE html>
<html>
    <head>
        <title>Edit <%= @genre.name %></title>
    </head>
    <body>
        <%= form_for(@genre) do |f| %>
            <%= f.label :name %>
            <%= f.text_field :name %>
            <br>
            <%= f.submit %>
        <% end %>
    </body>
</html>