<h1><%= @artist.name %> </h1>
<p> <%= @artist.bio %> </P>
<ul>
  <% @artist.songs.each do |song| %>
  <li>
    <a href=<%= song_path(song)%> >
      <%= song.name %>
    </a>
    in
    <a href=<%= genre_path(song.genre)%> >
      <%= song.genre.name %>
    </a>
  </li>
  <% end %>
</ul>
