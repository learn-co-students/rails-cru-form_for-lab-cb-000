<h1>Update Artist</h1>
<%= form_for @artist do |a| %>
  <%= a.label 'Name' %><br>
  <%= a.text_field :name %><br>

  <%= a.label 'Bio' %><br>
  <%= a.text_field :bio %><br>

  <%= a.submit "Update Artist" %>
<% end %>
