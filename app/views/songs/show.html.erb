<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Song Show Page</title>
  </head>
  <body>
    <h1><%= @song.name %></h1>
    <h3> <a href="<%= artist_path(@artist) %>"><%= @artist.name %></a> </h3>
    <h3> <a href="<%= genre_path(@genre) %>"><%= @genre.name %></a></h3>
  </body>
</html>
