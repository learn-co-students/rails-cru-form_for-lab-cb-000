<h1><%= @song.name %> </h1>

<% if @song.artist%>
  <h3>Artist:</h3>
    <%= link_to @song.artist.name, artist_path(@song.artist.id) %><br>
<% end %>

<% if @song.genre%>
  <h3>Genre:</h3>
  <%= link_to @song.genre.name, genre_path(@song.genre.id) %>
<%end%>
