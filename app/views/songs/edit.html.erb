<%= form_for(@song) do |f| %>
  <h3>Artist Form</h3>

  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :genre_id %>
  <%= f.text_area :genre_id %><br>

  <%= f.label :artist_id %>
  <%= f.text_area :artist_id %><br>

  <%= f.submit %>
<% end %>
